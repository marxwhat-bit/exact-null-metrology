# exact-null-metrology
A calibrated measurement instrument, not a method. It measures how much statistical dependence a parallel/masked decoder destroys or manufactures, on fields whose dependence structure is known **exactly** — every non-edge is independent by construction, not approximately, so any dependence found off the graph was created by the system under test.
